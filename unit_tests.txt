# test complete chuletas report
git co mingw64
chu > a.txt
git co eng_vars
chu > b.txt
diff a.txt b.txt

# test complete show config
git co mingw64
chu --show-config > a.txt
git co eng_vars
chu --show-config > b.txt
diff a.txt b.txt

# test frequent
git co mingw64
chu --frequent > a.txt
git co eng_vars
chu --frequent > b.txt
diff a.txt b.txt

# test deploy
cd ~/.cache/
mv chu chu.back.test
cd -
git co mingw64
ejecutar deploy como admin
cd -
mv chu chu.back.1
cd -
git co eng_vars
ejecutar deploy como admin
cd -
mv chu chu.back.2
diff chu.back.1 chu.back.2

# test update (must be run after test deploy)
rm -rf chu
mv chu.back.1 chu
cd -
git co mingw64
chu --update
cd -
mv chu chu.back.1.1
cd -
git co eng_vars
cd - 
mv chu.back.2 chu
chu --update
mv chu chu.back.2.2
diff chu.back.1.1 chu.back.2.2

# test search with menu

