x EDITOR variable not initialized
x files with wrong permissions (solve this in deploy)
x insert of commented lines in config database
x \r inserted into the valie of database settings because config file comes from windows
x one has to run config immediatly after deploy
x add gawk as a required dependency
- create a separate script to check dependencies with separated MINGW and non/MINGW sections
- deploy to /var/lib, including scripts, chuletas, cache and config
x add an option to clear LAST_GIT_TAG
x test clipboard option

